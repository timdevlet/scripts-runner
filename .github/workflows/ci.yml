name: CI

on:
  push:
    branches: [main]
    # Release tags are bare (0.1.2) per scripts/release.mjs; `v0.1.2` is accepted too.
    tags: ['v[0-9]*', '[0-9]*.[0-9]*.[0-9]*']
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint (typecheck + biome)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test

  build:
    name: Build ${{ matrix.name }}
    needs: [lint, test]
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            name: Windows
            # Single self-contained .exe — no installer, no admin rights.
            dist: npm run dist:portable:win -- --publish never
            artifact: windows-portable
            path: release/*.exe
          - os: macos-latest
            name: macOS
            # macOS has no "portable" target; a zipped .app is the equivalent
            # (unzip and run). Both arches, since the runner is arm64 and Intel
            # Macs would otherwise get nothing.
            dist: npm run dist:portable:mac -- --publish never
            artifact: macos-portable
            path: release/*.zip
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Build ${{ matrix.name }}
        run: ${{ matrix.dist }}
        env:
          # No signing certificates in CI: keep electron-builder from looking for a keychain
          # identity and failing the macOS build.
          CSC_IDENTITY_AUTO_DISCOVERY: false
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.path }}
          if-no-files-found: error
          retention-days: 30

  release:
    name: Publish release
    needs: build
    # Only publish on pushes to main (rolling "latest") or version tags (stable release).
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref_type == 'tag')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
      - name: Publish binaries to a GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          mapfile -t files < <(find dist -type f \( -name '*.exe' -o -name '*.zip' \) | sort)
          echo "Found ${#files[@]} portable builds:"
          printf '  %s\n' "${files[@]}"
          if [ "${#files[@]}" -eq 0 ]; then
            echo "::error::No portable builds were found in the artifacts."
            exit 1
          fi
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            # Stable, versioned release from the pushed tag.
            gh release create "$GITHUB_REF_NAME" "${files[@]}" \
              --title "$GITHUB_REF_NAME" \
              --generate-notes \
              --target "$GITHUB_SHA"
          else
            # Rolling prerelease that always reflects the latest commit on main.
            gh release delete latest --yes --cleanup-tag || true
            gh release create latest "${files[@]}" \
              --title "Latest (main)" \
              --prerelease \
              --target "$GITHUB_SHA" \
              --notes "Automated build from the latest commit on main (\`${GITHUB_SHA}\`)."
          fi
